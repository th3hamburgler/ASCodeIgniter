// create a remote git repository
git remote add origin git@github.com:th3hamburgler/ASCodeIgniter.git

// add file to local git repository
git add path/to/file.ext

// commit files added to local repository
git commit -m 'change message'

// push all 'commit' files to remote repository
git push origin master

// remove file from repo
git rm path/to/file.ext

// query git current status
git status